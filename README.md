## Task 1B: Data Cleaning
1. Remove Invalid Values
   More suitable for columns with marked ranges such as mood (0-10), arousal (-2 - 2) etc
2. Remove outliers (diff from invalid value), as range for time based columns can be from 0 to infinity, so outliers might be present.
3. Impute Missing Values
   Choose two different approaches, compare them and give reason why you went ahead with specific approach
   Another thing is to think about is what to with prolonged periods of data
